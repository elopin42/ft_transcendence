# --- Un proxy (que ce soit nginx, Next.js rewrites, ou un reverse proxy) sert à ce que le frontend appelle /api/auth/login au lieu de http://localhost:4000/auth/login
NEXT_PUBLIC_API_URL=http://localhost:4000